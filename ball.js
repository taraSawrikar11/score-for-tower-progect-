class Ball{
    constructor(x, y, radius,[options],[maxSides]) {
        var options = {
            isStatis:false,
            restitution:0.3,
            friction:0.5,
            density:1.2
        }
      //super(x,y,width,height);
      
    }
    
  };